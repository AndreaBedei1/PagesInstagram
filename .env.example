# ============================================================================
#  Instagram Content Engine - environment variables
#  Copy this file to ".env" and fill in real values. NEVER commit ".env".
#  Tokens are read ONLY from environment / .env, never from YAML or the DB.
# ============================================================================

# ---- Global run mode: dry_run | test | production --------------------------
ICE_MODE=dry_run

# ---- Meta / Instagram Graph API --------------------------------------------
# Graph API version, e.g. v23.0 (see README "Limiti API").
META_GRAPH_API_VERSION=v23.0

# Long-lived access token (valid 60 days, refreshable). Obtain per README.
# You may set one global token, or per-page tokens (preferred for 2 pages).
META_ACCESS_TOKEN=

# --- Per-page overrides (recommended). The page_id below matches accounts/*.yaml
# Instagram professional account numeric ID (the "IG user id" for /media calls)
ICE_MOTIVATIONAL_IT_IG_USER_ID=
ICE_MOTIVATIONAL_IT_ACCESS_TOKEN=

ICE_FAMOUS_QUOTES_IT_IG_USER_ID=
ICE_FAMOUS_QUOTES_IT_ACCESS_TOKEN=

# ---- App credentials (only needed to (re)generate / refresh tokens) --------
META_APP_ID=
META_APP_SECRET=

# ---- Public media hosting (Graph API requires PUBLIC https URLs) -----------
# The API downloads media from a URL you host; it cannot read local files.
# Base public URL that maps to the ./generated folder (see README "Pubblicazione").
ICE_PUBLIC_MEDIA_BASE_URL=

# ---- ComfyUI ---------------------------------------------------------------
ICE_COMFYUI_URL=http://127.0.0.1:8188
ICE_COMFYUI_LAUNCH_BAT=F:\AI\start_comfyui.bat
ICE_COMFYUI_OUTPUT_DIR=F:\AI\output

# ---- Dashboard (local only) ------------------------------------------------
ICE_DASHBOARD_HOST=127.0.0.1
ICE_DASHBOARD_PORT=8765
ICE_DASHBOARD_TOKEN=change-me-local-only
